/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["nft-mint-mini-app.vercel.app", "i.postimg.cc"], // دامنه‌های تصاویرت
  },
};

module.exports = nextConfig;